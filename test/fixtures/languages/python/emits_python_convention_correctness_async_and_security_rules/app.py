import os, sys
value = eval(input())
from module import *

def BadName(items=[]):
    assert items
    requests.get("https://example.test")

async def load():
    time.sleep(1)

password = "secret"
data = yaml.load(payload)
query = f"SELECT * FROM users WHERE id = {value}"
handle = open("data.txt")
tempfile.mktemp()
