import { readFileSync, R_OK, W_OK } from 'node:fs'
const value: any = JSON.parse(input);
console.log(value);
debugger;
eval(input);
element.innerHTML = input;
fetch("/api");
const password = "secret";
