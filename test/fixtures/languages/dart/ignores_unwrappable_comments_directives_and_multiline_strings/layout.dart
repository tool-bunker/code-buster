// This documentation line is intentionally much longer than the configured limit.
import 'package:analyzer_plugin/utilities/change_builder/change_builder_core.dart';
final message = r"""
This multiline string content is intentionally much longer than the configured limit.
""";
final value = callWithSeveralArguments(first, second, third, fourth);
