echo ("<", config, result)
echo source, " <- ", target
echo occupied < limit, " peak memory"
echo "<", value, ">"
echo "<msg> ", message
if signum < 0: write(output, '-')
let parsed = parseXml("<user>" & userName & "</user>")
let fixture = "<user>" & userName & "</user>"
echo "<user>", xmltree.escape(userName), "</user>"
# echo "<user>", userName, "</user>"
