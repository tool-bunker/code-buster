KB = {
    "password-reset": "To reset your password, open Settings.",
}
_TAMPER = ("disable", "turn off", "delete logs", "clear audit")

password = "actual credential"
query = f"DELETE FROM jobs WHERE id = {job_id}"
