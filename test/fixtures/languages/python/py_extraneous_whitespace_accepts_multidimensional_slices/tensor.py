column = output[index, :]
window = output[:, :]
invalid = call(value , other)
while chunk := response.read(1024 * 1024):
    consume(chunk)
if element := find_element(app, name):
    activate(element)
# If true, '()' is appended to :func: references.
