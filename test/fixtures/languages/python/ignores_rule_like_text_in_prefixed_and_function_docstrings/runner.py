r"""Module documentation with ( spacing).
if ready: os.system(command)
"""
import os

def run():
    """The result of os.system(command).

    if ready: popen(command)
    """
    return os.system(command)
