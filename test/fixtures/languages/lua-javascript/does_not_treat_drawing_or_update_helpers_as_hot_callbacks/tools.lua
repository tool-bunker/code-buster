function drawing_with_brushes()
  print("drawing")
  local expectedImages = {
    { 0, 0, 0, 0 },
  }
end
function M.update_base_lvim()
  print("Checking for updates")
  local args = { "merge", "--ff-only" }
end
