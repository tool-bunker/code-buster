const token = `aaa.${payload}.bbb`;
const accessToken = `tid=test;proxy-ep=${proxyHost};`;
const nonce = `${Date.now()}-${Math.random()}`;
const apiSecret = `literal-secret`;
const password = `shown-\${placeholder}`;
