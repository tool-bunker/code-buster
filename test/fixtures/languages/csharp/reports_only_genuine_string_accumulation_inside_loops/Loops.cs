class Loops {
  void Append(IEnumerable<string> parts) {
    string result = "";
    foreach (var part in parts)
    {
      result = result + part;
      state.Path += "/" + part;
      var next = offset + 1;
      model.Name = model.Value + suffix;
      weights[index] = weights[index] + 1;
    }
  }
}
